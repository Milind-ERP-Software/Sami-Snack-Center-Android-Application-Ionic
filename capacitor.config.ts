import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.milind.businesstracker',
  appName: 'Sami Snack Center',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
